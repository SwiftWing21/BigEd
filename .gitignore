# ── Secrets / credentials ─────────────────────────────────────────────────────
.env
.env.*
.secrets
*.secrets
secrets.*

# ── Python bytecode ───────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.Python

# ── Virtual environments ──────────────────────────────────────────────────────
.venv/
venv/
env/
ENV/

# ── Documentation — private by default, whitelist product files ──────────────
*.md
!README.md
!CONTRIBUTING.md
!SETUP.md
!CLAUDE.md
!ROADMAP.md
!OPERATIONS.md
!FRAMEWORK_BLUEPRINT.md
!AUDIT_TRACKER.md
!fleet/CLAUDE.md
!BigEd/STABILITY_GUIDE.md
!docs/WHAT_IS_BIGED.md
!docs/superpowers/**/*.md
!autoresearch/README.md
!.github/PULL_REQUEST_TEMPLATE.md
!docs/flowcharts/README.md
!docs/screenshots/README.md
!.claude/skills/**/*.md
!fleet/templates/**/*.md

# ── Fleet — runtime databases ─────────────────────────────────────────────────
fleet/fleet.db
fleet/fleet.db-shm
fleet/fleet.db-wal
fleet/rag.db
fleet/rag.db-shm
fleet/rag.db-wal

# ── Fleet — live status (regenerated on start) ────────────────────────────────
fleet/.boot_status.json
# ── Fleet — ephemeral HITL response files ─────────────────────────────────────
fleet/hitl-responses/

# ── Launcher — single-instance lock ───────────────────────────────────────────
BigEd/launcher/data/biged.lock

# ── Fleet — misplaced VS Code configs (Gemini artifacts) ─────────────────────
fleet/launch.json
fleet/tasks.json
fleet/skills/launch.json
fleet/skills/tasks.json

# ── Fleet — TLS certs (auto-generated, never commit) ─────────────────────────
fleet/certs/

# ── Fleet — logs ──────────────────────────────────────────────────────────────
fleet/logs/*
!fleet/logs/.gitkeep

# ── Fleet — generated knowledge (runtime output, not source) ──────────────────
fleet/knowledge/leads/
fleet/knowledge/reports/
fleet/knowledge/summaries/
fleet/knowledge/discussion/
fleet/knowledge/code_discussion/
fleet/knowledge/security/
fleet/knowledge/marketing/
fleet/knowledge/design/
fleet/knowledge/code_writes/
fleet/knowledge/code_drafts/
fleet/knowledge/code_reviews/
fleet/knowledge/fma_reviews/
fleet/knowledge/quality/
fleet/knowledge/chains/
fleet/knowledge/biged_agents/
fleet/knowledge/diffusion/
fleet/knowledge/ingests/
fleet/knowledge/ingest_cache/
fleet/pids/
fleet/knowledge/plans/
fleet/knowledge/flashcards.jsonl
fleet/knowledge/code_index.jsonl
fleet/knowledge/search_waterfall.jsonl
fleet/knowledge/discard_tracker.json

# ── Launcher — build artifacts ────────────────────────────────────────────────
BigEd/launcher/dist/
BigEd/launcher/build/

# ── Launcher — PyInstaller spec files (contain absolute paths, regenerated) ───
BigEd/launcher/*.spec

# ── Launcher — CRM / tools SQLite database (user runtime data) ───────────────
BigEd/launcher/data/

# ── Claude Code local settings (permissions, not project instructions) ────────
.claude/
!.claude/skills/**

# ── Local machine config (personal hardware/env/MCP — never committed) ────────
CLAUDE.USER.md
SESSION_HANDOFF.md
.mcp.json
push.sh

# ── Fleet — runtime files (regenerated every run) ────────────────────────────
fleet/.drders_heartbeat
fleet/.supervisor_heartbeat
fleet/.cost_anomaly_throttle
fleet/knowledge/screenshots/

# ── Launcher — runtime data (boot timing, caches) ────────────────────────────
BigEd/launcher/ui/data/

# ── Docker (local dev orchestration) ─────────────────────────────────────────
docker-compose.yml

# ── MCP context mirror (copies of root .md files for container access) ───────
education-context/

# ── Dev reference docs (moved to MCP server, not tracked in git) ──────────
CLAUDE.C_REVIEW.md
CLAUDE.CHATS.md
GEMINI.md
MACHINE_PROFILE.md
CROSS_PLATFORM.md
ROADMAP_v014_v020.md
ROADMAP_v020_v030.md
BigEd/architecture_comparison.md
BigEd/build_reference.md
BigEd/dev_environment.md
BigEd/fleet_blueprints.md
BigEd/fleet_commands.md
BigEd/qa_token_blueprint.md
BigEd/CONSOLE_UPGRADE_PLAN.md
BigEd/SECURITY_COMPLIANCE_BLUEPRINT.md
BigEd/research/

# ── Factorio module (distributed via Module Hub, not in public repo) ──────
fleet/factorio/
fleet/factorio_blueprint.py
fleet/skills/factorio_*.py
fleet/idle_curricula/factorio_*.toml
fleet/templates/components/_factorio.html
fleet/tests/factorio/
tests/factorio/
docs/superpowers/plans/*factorio*
docs/superpowers/specs/*factorio*
tests/test_action_translator.py
tests/test_agent_brain.py
tests/test_bridge_api.py
tests/test_bridge_config.py
tests/test_cadence.py
tests/test_curriculum.py
tests/test_curriculum_manager.py
tests/test_experiment_runner.py
tests/test_experiment_scorer.py
tests/test_focus_toggle.py
tests/test_lua_installer.py
tests/test_plan_queue.py
tests/test_prompt_loader.py
tests/test_rcon_client.py
tests/test_state_parser.py
tests/test_world_model.py
# ── Fleet runtime state ──────────────────────────────────────────────────
fleet/hw_state.json
fleet/tmp*.json
biged-rs/.supervisor_heartbeat
biged-rs/hw_state.json
biged-rs/fleet.toml
demo.py
.superpowers/
docs/screenshots/*.png
fleet/_graveyard/
fleet/knowledge/evolution/
fleet/knowledge/marathon/
fleet/knowledge/swarm/
fleet/knowledge/consensus/
fleet/knowledge/datasets/
fleet/knowledge/ml_results/
fleet/knowledge/agent_cards.json
fleet/data/

# ── IDE — MCP config (may contain credentials; launch.json + tasks.json stay tracked)
.vscode/mcp.json

# ── Junk / temp artifacts (should never be tracked anywhere) ───────────────
*.bak
*.tmp
*.swp
.clone/
ziXHiPfz
model-manager.plugin
repo-descriptions.txt

# ── Fleet — runtime state flags ──────────────────────────────────────────────
fleet/.queue_paused
fleet/.walkthrough_done
fleet/.port

# ── Audit databases / baselines (runtime, not source) ───────────────────────
*.audit.db
audit.db
audit_baseline.json
biged_audit.db
biged_baseline.json
.two-brain-audit.json

# ── Audit snapshots (exported runtime data, not source) ─────────────────────
fleet/audit-snapshots/


# ── Fleet — additional generated knowledge dirs ──────────────────────────────
fleet/knowledge/digests/
fleet/knowledge/flywheel/
fleet/knowledge/freshness/
fleet/knowledge/outcomes/
fleet/knowledge/prompt_optimization/
fleet/knowledge/sync/

# ── Fleet — nested fleet/ dir (accidental) ───────────────────────────────────
fleet/fleet/

# ── Autoresearch — runtime logs ──────────────────────────────────────────────
autoresearch/run.log

# ── Draft posts / personal docs (not for public) ────────────────────────────
reddit-*.md
BigEd_Architecture_Report.docx

# ── Screenshots (tracked via .gitignore in docs/screenshots/) ────────────────
docs/screenshots/*.png

# ── OS junk ───────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# ── Team orchestrator runtime state ───────────────────────────────────────────
.claude/skills/team-orchestrator/team-state/.team-log*

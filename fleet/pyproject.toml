[project]
name = "fleet"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "anthropic>=0.40.0",
]

[project.optional-dependencies]
gpu = [
    "nvidia-ml-py>=12.0.0",
    "pyamdgpuinfo>=2.1.0; sys_platform == 'linux'",
]

# coder_1 — software architect perspective
# Produces: code_discuss contributions, code_review reports, skill_draft files

# ── Code discussions ───────────────────────────────────────────────────────────

[[tasks]]
type = "code_discuss"
[tasks.payload]
agent_name = "coder_1"
role_perspective = "software architect"
topic = "fleet horizontal scaling — sharding task queues across multiple supervisors"
round = 1

[[tasks]]
type = "code_discuss"
[tasks.payload]
agent_name = "coder_1"
role_perspective = "software architect"
topic = "skill pipeline design — chaining skills in DAG sequences with typed I/O contracts"
round = 1

[[tasks]]
type = "code_discuss"
[tasks.payload]
agent_name = "coder_1"
role_perspective = "software architect"
topic = "multi-backend orchestration — LocalBackend ABC vs provider-level abstraction tradeoffs"
round = 1

[[tasks]]
type = "code_discuss"
[tasks.payload]
agent_name = "coder_1"
role_perspective = "software architect"
topic = "worker lifecycle management — crash backoff, health probes, and graceful shutdown ordering"
round = 1

[[tasks]]
type = "code_discuss"
[tasks.payload]
agent_name = "coder_1"
role_perspective = "software architect"
topic = "DAG task graph optimization — conditional edges, priority inheritance, and cycle detection"
round = 1

[[tasks]]
type = "code_discuss"
[tasks.payload]
agent_name = "coder_1"
role_perspective = "software architect"
topic = "dual supervisor separation — supervisor.py vs hw_supervisor.py boundary and shared state via hw_state.json"
round = 1

[[tasks]]
type = "code_discuss"
[tasks.payload]
agent_name = "coder_1"
role_perspective = "software architect"
topic = "knowledge layer architecture — RAG indexing, file-based artifacts, and DB-backed metadata coexistence"
round = 1

# ── Code reviews ───────────────────────────────────────────────────────────────

[[tasks]]
type = "code_review"
[tasks.payload]
agent_name = "coder_1"
perspective = "software architect"
file = "supervisor.py"
focus = "module boundaries, worker lifecycle coupling, process control abstraction"

[[tasks]]
type = "code_review"
[tasks.payload]
agent_name = "coder_1"
perspective = "software architect"
file = "dashboard.py"
focus = "endpoint organization, Blueprint split candidates, data access patterns"

[[tasks]]
type = "code_review"
[tasks.payload]
agent_name = "coder_1"
perspective = "software architect"
file = "providers.py"
focus = "backend abstraction coherence, model routing logic, HA fallback cascade design"

[[tasks]]
type = "code_review"
[tasks.payload]
agent_name = "coder_1"
perspective = "software architect"
file = "db.py"
focus = "schema design, connection management, DAG query patterns, migration strategy"

# ── Skill drafts ───────────────────────────────────────────────────────────────

[[tasks]]
type = "skill_draft"
[tasks.payload]
agent_name = "coder_1"
perspective = "software architect"
skill_name = "architecture_lint"
description = "Scans fleet Python modules for architectural violations: circular imports, god-object methods (>200 LOC), missing ABC implementations, and boundary leaks between layers (skills importing from launcher, dashboard importing from supervisor)."
inputs = "directory, max_files, violation_types"
outputs = "structured violation report saved to knowledge/quality/arch_lint_<date>.md"

[[tasks]]
type = "skill_draft"
[tasks.payload]
agent_name = "coder_1"
perspective = "software architect"
skill_name = "dependency_graph"
description = "Builds an import dependency graph of fleet modules using AST analysis. Detects circular dependencies, identifies coupling hotspots, and generates a DOT-format visualization. Tracks graph complexity over time."
inputs = "root_directory, exclude_patterns"
outputs = "dependency graph JSON + DOT file saved to knowledge/quality/dep_graph_<date>.json"

[[tasks]]
type = "skill_draft"
[tasks.payload]
agent_name = "coder_1"
perspective = "software architect"
skill_name = "api_benchmark"
description = "Benchmarks fleet dashboard API endpoints by sending timed requests and recording latency percentiles (p50/p95/p99). Compares against baseline from previous runs to detect regressions."
inputs = "endpoints list, iterations_per_endpoint, baseline_path"
outputs = "benchmark report saved to knowledge/quality/api_bench_<date>.md"

# ── Code write ─────────────────────────────────────────────────────────────────

[[tasks]]
type = "code_write"
[tasks.payload]
instructions = "Create a Python utility that analyzes fleet module boundaries by scanning imports across supervisor.py, dashboard.py, worker.py, providers.py, and db.py. Report any cross-layer imports that violate the intended architecture (e.g., dashboard importing directly from supervisor internals)."
create_files = ["module_boundary_check.py"]

[[tasks]]
type = "code_write"
[tasks.payload]
instructions = "Create a Python script that reads fleet.db and hw_state.json to produce a single-page health summary: agent count by status, task throughput (last 1h/24h), model performance (tok/s avg), and top 5 skills by volume. Output as formatted markdown."
create_files = ["fleet_health_summary.py"]

# ── Code write reviews ────────────────────────────────────────────────────────

[[tasks]]
type = "code_write_review"
[tasks.payload]
agent_name = "coder_1"
perspective = "software architect"

# ── FMA review & enhancement discussion ──────────────────────────────────────

[[tasks]]
type = "fma_review"
[tasks.payload]
agent_name = "coder_1"
perspective = "software architect"
file = "launcher.py"
focus = "module structure, GUI architecture, extensibility points, Settings/Boot/Console separation"
mode = "both"
round = 1

[[tasks]]
type = "fma_review"
[tasks.payload]
agent_name = "coder_1"
perspective = "software architect"
file = "build.py"
focus = "build pipeline design, platform detection, PyInstaller configuration, production flag handling"
mode = "both"
round = 2

[[tasks]]
type = "fma_review"
[tasks.payload]
agent_name = "coder_1"
perspective = "software architect"
focus = "fleet component boundaries — where supervisor/dashboard/worker/skills interfaces should be formalized as ABCs"
mode = "discuss"
round = 3

# ── Codebase indexing ──────────────────────────────────────────────────────────

[[tasks]]
type = "code_index"
[tasks.payload]
directory = "fleet"
max_files = 25
